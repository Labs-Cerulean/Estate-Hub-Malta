; Increase maximum upload size to 500MB
upload_max_filesize = 500M
post_max_size = 500M

; Increase memory limit and timeouts for handling large video/dwg files
memory_limit = 512M
max_execution_time = 300
max_input_time = 300

; Increase input variables limit for massive forms (like Mobilisation grids)
max_input_vars = 10000
